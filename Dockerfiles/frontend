FROM ruby:2.4.2

EXPOSE 80

RUN git clone https://github.com/alphagov/verify-frontend.git

WORKDIR verify-frontend

RUN rm .env

RUN bundle check || bundle install

CMD bundle exec puma -e development -p 80
